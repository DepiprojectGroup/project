apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: ordering-system
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: backend
